<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.navi.board.photo.dao.PhotoDAO">
<resultMap type="BoardPostVO" id="boardPostVOMap" autoMapping="true">
	<id property="bpId" column="BP_ID"/>
	<association property="emp" javaType="EmpVO" autoMapping="true"/>
</resultMap>

<!-- 	<sql id="searchFrag">
		<trim prefix="WHERE (" suffix=")" prefixOverrides="AND">
			<if test="not detailCondition.empty">
				<if
					test="@org.apache.commons.lang3.StringUtils@isNotBlank(detailCondition.bpId)">
					BP_ID = #{detailCondition.bpId} AND
				</if>
				<if
					test="@org.apache.commons.lang3.StringUtils@isNotBlank(detailCondition.empId)">
					EMP_ID = #{detailCondition.empId} AND
				</if>
				<if
					test="@org.apache.commons.lang3.StringUtils@isNotBlank(detailCondition.bpCn)">
					INSTR(BP_CN, #{detailCondition.bpCn}) > 0 AND
				</if>
			</if>
		</trim>
	</sql> -->
	<select id="selectPhoto" resultType="BoardPostVO" parameterType="String">
		SELECT 
            BOARD_POST.BP_ID,
            BOARD_POST.EMP_ID,
            BOARD_POST.BP_MOD_ID,
            BOARD_POST.BRD_ID,
            BOARD_POST.BP_TITLE,
            BOARD_POST.BP_CN,
            BOARD_POST.BP_REG_DT,
            BOARD_POST.BP_DEL_DT,
            BOARD_POST.BP_OP_YN,
            BOARD_POST.BP_CNT,
            BOARD_POST.BP_MOD_DT,
            BOARD_POST.BP_SHR_YN,
            BOARD_POST.BP_NTC_ID,
            BOARD_POST.BP_QNA_AS,
            EMP.EMP_NAME
        FROM 
            BOARD_POST
        INNER JOIN 
            EMP ON (BOARD_POST.EMP_ID = EMP.EMP_ID)
        WHERE
            BOARD_POST.BRD_ID = 'BN0004' AND BOARD_POST.BP_ID = #{bpId}
	</select>
	
	
	
	<select id="selectTotalRecord"  resultType="int" parameterType="PaginationInfo">
		SELECT COUNT(*)
		FROM BOARD_POST
<!-- 		<include refid="searchFrag" /> -->
	</select>
	
	<select id="selectPhotoList"  resultMap="boardPostVOMap" parameterType="PaginationInfo">
		SELECT B.*
	FROM (
	    	SELECT ROWNUM RNUM, A.*
	    FROM (
	        SELECT 
	            BOARD_POST.BP_ID,
	            BOARD_POST.EMP_ID,
	            BOARD_POST.BP_MOD_ID,
	            BOARD_POST.BRD_ID,
	            BOARD_POST.BP_TITLE,
	            BOARD_POST.BP_CN,
	            BOARD_POST.BP_REG_DT,
	            BOARD_POST.BP_DEL_DT,
	            BOARD_POST.BP_OP_YN,
	            BOARD_POST.BP_CNT,
	            BOARD_POST.BP_MOD_DT,
	            BOARD_POST.BP_SHR_YN,
	            BOARD_POST.BP_NTC_ID,
	            BOARD_POST.BP_QNA_AS,
	            EMP.EMP_NAME
	        FROM 
	            BOARD_POST
	        INNER JOIN 
	            EMP ON (BOARD_POST.EMP_ID = EMP.EMP_ID)
	        WHERE
	        	BOARD_POST.BRD_ID = 'BN0004'
	    	) A
	) B
		<![CDATA[
			WHERE RNUM >= #{startRow} AND RNUM <= #{endRow}
		]]>
	
			
	</select>
<!-- 	<update id="updatePhoto" parameterType="BoardPostVO"> -->
<!-- 		UPDATE  -->
<!-- 			BOARD_POST -->
<!-- 		SET -->
<!-- 			BP_ID = #{bpId,jdbcType=CHAR} -->
<!-- 			, EMP_ID = #{empId,jdbcType=CHAR} -->
<!-- 			, BP_MOD_ID = #{bpModId,jdbcType=CHAR} -->
<!-- 			, BRD_ID = #{brdId,jdbcType=CHAR} -->
<!-- 			, BP_TITLE = #{bpTitle,jdbcType=VARCHAR} -->
<!-- 			, BP_CN = #{bpCn,jdbcType=CLOB} -->
<!-- 			, BP_REG_DT = #{bpRegDt,jdbcType=TIMESTAMP(6)} -->
<!-- 			, BP_DEL_DT = #{bpDelDt,jdbcType=TIMESTAMP(6)} -->
<!-- 			, BP_OP_YN = #{bpOpYn,jdbcType=CHAR} -->
<!-- 			, BP_CNT = #{bpCnt,jdbcType=NUMERIC} -->
<!-- 			, BP_MOD_DT = #{bpModDt,jdbcType=TIMESTAMP(6)} -->
<!-- 			, BP_SHR_YN = #{bpShrYn,jdbcType=CHAR} -->
<!-- 			, BP_NTC_ID = #{bpNtcId,jdbcType=CHAR} -->
<!-- 			, BP_QNA_AS = #{bpQnaAs,jdbcType=CLOB} -->
<!-- 		WHERE bp_id = #{bpId} -->
	
<!-- 	</update> -->
</mapper>