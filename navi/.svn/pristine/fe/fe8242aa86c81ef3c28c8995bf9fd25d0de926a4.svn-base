package kr.or.navi.todaylist.dao;

import java.util.List;

import org.apache.ibatis.annotations.Mapper;
import org.apache.ibatis.annotations.Param;

import kr.or.navi.common.paging.PaginationInfo;
import kr.or.navi.vo.ProjectVO;
import kr.or.navi.vo.TaskVO;
import kr.or.navi.vo.TodayListVO;

@Mapper
public interface TodayListDAO {
	//담당자가 본인인 일감 전체조회
	public List<TodayListVO> selectTaskList(PaginationInfo paging,@Param("taskManId") String taskManId);
	public int selectTaskListTotalRecord(PaginationInfo paging,@Param("taskManId") String taskManId);

	
	
	/*
	 * //일감전체행수 조회 레코드 public int selectTotalRecord(PaginationInfo paging); //일감 단건
	 * 조회 (상세) //public TodayListDAO selecTaskList(@Param("taskId") String taskId);
	 *
	 * //프로젝트 단건조회 public List<TodayListVO> selectProjectList(@Param("paging")
	 * PaginationInfo paging,@Param("proId") String proId); //프로젝트 단건에대한 전체행수 조회 레코드
	 * public int selectProjectTotalRecord(@Param("paging") PaginationInfo
	 * paging,@Param("proId")String proId);
	 * 
	 * 
	 * //오늘의 일정 전체조회 public List<TodayListVO> selectTodayList();
	 * 
	 * //오늘의 일정 insert public int insertTodayList(TodayListVO todayVO); //오늘의 일정
	 * update : 소요시간, 예상시간 변경 및 일정 적용시 완료버튼누르면 발생 public int
	 * updateTodayList(TodayListVO todayVO);
	 */
	
	
}
