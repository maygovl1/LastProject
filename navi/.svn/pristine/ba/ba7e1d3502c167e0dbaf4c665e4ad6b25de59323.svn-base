package kr.or.navi.todaylist.dao;

import java.util.List;

import org.apache.ibatis.annotations.Mapper;
import org.apache.ibatis.annotations.Param;

import kr.or.navi.common.paging.PaginationInfo;
import kr.or.navi.vo.TaskVO;
import kr.or.navi.vo.TodayListVO;

@Mapper
public interface TodayListDAO {
	//일감 전체조회
	public List<TodayListVO> selectTaskList(PaginationInfo paging);
	//일감전체행수 조회 레코드
	public int selectTotalRecord(PaginationInfo paging);
	//프로젝트 단건조회
	public List<TodayListVO> selectProjectList(@Param("paging") PaginationInfo paging,@Param("proId") String proId);
	//프로젝트 단건에대한 전체행수 조회 레코드
	public int selectProjectTotalRecord(@Param("paging") PaginationInfo paging,@Param("proId")String proId);
	
	
	//오늘의 일정 전체조회
	public List<TodayListVO> selectTodayList();

	//오늘의 일정 insert 
	public int insertTodayList(TodayListVO todayVO);
	//오늘의 일정 update : 소요시간, 예상시간 변경 및 일정 적용시 완료버튼누르면  발생
	public int updateTodayList(TodayListVO todayVO);
	
	
}
